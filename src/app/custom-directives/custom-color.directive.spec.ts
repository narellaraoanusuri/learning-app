import { CustomColorDirective } from './custom-color.directive';

describe('CustomColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomColorDirective();
    expect(directive).toBeTruthy();
  });
});
